{
  "name": "countdown",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "npm update",
    "deploy": "gh-pages -d ."
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  },
  "homepage": "https://github.com/Juan-87/countdown/"
}
